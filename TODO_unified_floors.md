# TODO: объединение помещений 1/2 (вариант а)

## Цель
- Оставить **оба этажа и их отображение** (переключатель этажей остаётся).
- Но считать, что это **одно здание/помещение** для навигации: маршрут строится с учётом перехода по лестнице между этажами.
- Вход — только на **этаже 1**.
- Лестница — по центру здания: используется точка (node) **перехода** между этажами.

## Важные изменения
1. **Геометрия/данные входа**
   - В `floorsData[1].zones` убрать/не использовать «вход» как отдельную зону, либо явно завести отдельный `startNode` для этажа 1.
   - Задать координаты входа (по текущей svg: этаж 1 entrance rect x="220" y="590" width="120" height="30").

2. **Лестница (единая по центру здания)**
   - Добавить в данных графа **одну** лестницу (или два её конца, но с совпадающим по центру x):
     - `ladderNodeFloor1` (узел лестницы на этаже 1)
     - `ladderNodeFloor2` (узел лестницы на этаже 2)
   - Утвердить координаты центра лестницы на каждом этаже (в терминах текущих SVG координат).

3. **Граф коридоров**
   - Вместо предположения, что «строение 1 и строение 2 — разные помещения», граф должен содержать узлы для обоих этажей.
   - Рёбра:
     - внутри этажа 1 (между коридорными узлами)
     - внутри этажа 2
     - вертикальный переход: edge между `ladderNodeFloor1` и `ladderNodeFloor2`.

4. **Маршрут должен управлять этажами**
   - При построении пути 1→2, маршрут должен:
     - при первом отрезке рисовать/показывать polyline на этаже 1
     - затем переключать этаж на 2 и рисовать остаток
   - При необходимости можно рисовать весь маршрут, но учитывая разные overlay — минимум нужен **корректный показ на активном этаже**.

5. **Интеграция с текущим flyTo**
   - Пока граф/route отсутствует, логика `flyToZone`/`flyToBooth` будет временно сохранена.
   - Позже при готовности route: если цель — zone/booth, то вместо `map.flyTo` строить маршрут и рисовать polyline.

## Примечания
- Вариант а: отображение этажей и их отдельные SVG остаются, но «логически» здание единое благодаря переходу по лестнице.

